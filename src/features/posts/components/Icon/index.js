import { HomeOutlined } from "@mui/icons-material";
import { BaseIcon } from "../../../../components/BaseIcon";

const Icon = ({ color }) => (
  <BaseIcon Icon={HomeOutlined} color={color} />
);

export { Icon };
